#!/usr/bin/env ruby

require 'telegram/bot'
require_relative '../lib/app_configurator'

Telegram::Bot::Client.run(token) do |bot|
  bot.listen do |message|
    bot.api.send_message(chat_id: message.chat.id, text: "Welcome, I'm Fritz the librarian Bot")
  end
end
